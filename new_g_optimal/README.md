## Scripts for G optimal detection

For Lucia:

The `go_sim_hpc.py` is the template file of doing brain simulation with SJ3D local model

The `main.py` file is the script I used to compute the G optimal

